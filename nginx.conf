server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  # SPA-fallback: alle onbekende paden → index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
  # geen indexering
  add_header X-Robots-Tag "noindex, nofollow" always;
}
